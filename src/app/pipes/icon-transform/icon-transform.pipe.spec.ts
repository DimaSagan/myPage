import { IconTransformPipe } from './icon-transform.pipe';

describe('IconTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new IconTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
