import { DelayInViewDirective } from './delay-in-view.directive';

describe('DelayInViewDirective', () => {
  it('should create an instance', () => {
    const directive = new DelayInViewDirective();
    expect(directive).toBeTruthy();
  });
});
